import 'package:flutter/material.dart';

class PercentageGraph extends StatelessWidget {
  const PercentageGraph({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
